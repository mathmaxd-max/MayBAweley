---
import BaseLayout from '../layouts/BaseLayout.astro';
import { author } from '../data/author';
---

<BaseLayout
  title="Kontakt – May B. Aweley"
  description="Kontaktieren Sie May B. Aweley über Social Media"
>
  <div class="bg-gradient-to-b from-neutral-light to-white py-12">
    <div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8">
      <h1 class="text-4xl md:text-5xl font-serif font-bold text-primary mb-8 text-center">
        Kontakt
      </h1>
      
      <div class="card p-8 text-center">
        <p class="text-lg text-text/90 mb-6">
          Für Anfragen, Interviewanfragen oder einfach nur zum Austausch können Sie {author.name} über die Social Media Kanäle erreichen.
        </p>
        
        <div class="space-y-4">
          {author.sameAs && author.sameAs.length > 0 ? (
            <ul class="flex flex-col sm:flex-row gap-4 justify-center items-center">
              {author.sameAs.map((url) => {
                const isFacebook = url.includes('facebook.com');
                const label = isFacebook ? 'Facebook' : new URL(url).hostname.replace('www.', '');
                return (
                  <li set:key={url}>
                    <a
                      href={url}
                      target="_blank"
                      rel="noopener noreferrer"
                      class="btn-primary inline-flex items-center gap-2"
                    >
                      {isFacebook && (
                        <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                          <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
                        </svg>
                        )}
                      {label}
                    </a>
                  </li>
                );
              })}
            </ul>
          ) : (
            <p class="text-text/70">Social Media Links werden hier angezeigt, sobald verfügbar.</p>
          )}
          
          <div class="mt-8 p-4 bg-neutral-light rounded-lg">
            <p class="text-sm text-text/70">
              <strong>Hinweis:</strong> Aufgrund der Verwendung eines Pseudonyms werden persönliche Kontaktdaten nicht öffentlich gemacht. 
              Bitte nutzen Sie die Social Media Kanäle für die Kommunikation.
            </p>
          </div>
        </div>
      </div>
      
      <div class="mt-8 text-center">
        <a href="/" class="text-primary hover:text-primary-dark transition-colors">
          ← Zurück zur Startseite
        </a>
      </div>
    </div>
  </div>
</BaseLayout>
